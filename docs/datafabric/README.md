# Data Fabric — `Ticket` (UI: "Support Ticket") evidence

Live proof of the Data Fabric entity behind SESAP (item 4 of the challenge — *export or screenshots of the entity design*).

| File | What it is |
|---|---|
| [`Ticket_export.csv`](Ticket_export.csv) | Live export of the entity records (16 tickets) — the real data the app and robot read/write |
| `entity-overview.png` | Screenshot: entity **Overview** — all fields (Reference, Subject, …, Assignment Status) |
| `entity-data.png` | Screenshot: entity **Data** grid — the ticket records |

Full field-by-field schema (types, required, unique, write‑back fields) is in [`../DATA_FABRIC_SCHEMA.md`](../DATA_FABRIC_SCHEMA.md) and the visual [`../DATA_FABRIC_ENTITY_SCHEMA.html`](../DATA_FABRIC_ENTITY_SCHEMA.html).
